# 给 agent 的项目说明

**这份文件的作用**：让你不必从零认识这个项目。每次任务都重新读一遍代码去
理解「这是什么、为什么这么写」是纯浪费 —— 而这个项目本身就是为了消灭浪费。

保持**短**。这份文件每次任务都会进你的上下文，写长了就是每次都在烧额度。
只写「不看会做错」的东西，不写代码里一眼能看出来的。

## 第 0 条：不允许弄虚作假

老板 2026-08-23 常设指示。全文见 `docs/no-faking.md`，和任何别的规矩冲突时这条优先。

- 「已完成 / 已修复 / 已验证」的断言背后必须有**能复现的实跑证据**；给不出就如实说没验证。
- 改「说法」不算修问题（改名字、改文案、改日志措辞 ≠ 修好）。
- 测试必须**变异验证**（把实现改坏，确认测试真的会红），没红过的测试不算覆盖。
- 端到端才算数：代码写了 ≠ 调得起来 ≠ 老板在手机上看得见。
- 不确定就说不确定；发现自己之前说错了，主动纠正。

## 这是什么

一个人同时订阅了 Claude、Codex、GLM、Kimi、Qwen、MiniMax、火山方舟。
每家的额度窗口、计量口径、重置时间都不同，而且**不用就作废**。
这套工具把所有平台的用量汇总到一处，再按「谁还有额度、谁擅长这类活」
派任务，让额度别白白过期。

- `LLMQuotaCore` —— 采集适配器、额度引擎、上限学习、任务调度、跨机 mTLS
- `llmq` —— 命令行
- `LLMQuotaBarApp` —— macOS 菜单栏

## 动手前先看

- `STATUS.md` —— 哪些做完了、哪些没有、哪些条目已作废。

  里面「最近落地」那段是**落地时自动写的**（`llmq work log` 能单独看），
  你不用管。**要你写的是它写不出来的那部分：为什么这么做、还差什么。**
  改了好几个文件却什么都没留下，进度里会标一个「说明待补」——
  那不是罚你，是让缺口本身可见，省得下一个人从 diff 里反推。
- `SECURITY.md` —— 无人值守执行的边界。它承诺的每一条都必须在代码里真实存在。

## 几条会被反复踩的规矩

**零第三方依赖。** 只用 Foundation / Network / Security，加系统自带的
`/usr/bin/openssl`、`/usr/bin/sqlite3`。这不是洁癖：这套东西要在多台机器上
长期无人值守地跑，每多一个依赖就多一处会在某次系统升级后悄悄坏掉的地方。

**跨版本、跨机器传的结构一律手写 `decodeIfPresent`。** 合成解码器对新增字段
零容忍，而**给属性写默认值救不了它** —— 缺键照样抛 `keyNotFound`，整条记录
解不出来，然后**静默消失**。这个坑在这个项目里踩过五次。

**版本号取集群里最旧那台的下限。** `swift-tools-version` 抬到 6.1 之后开发机
照样能编，而另一台 Swift 6.0.3 的机器上**所有验证失败**。同理，
`switch` 对 `Bool?` 要写 `.some(true)/.some(false)/.none`，旧编译器不认
`true/false/nil` 是穷尽的。

**诊断代码不能死在它要诊断的那件事上。** `Bundle.module` 找不到资源就
`fatalError`，于是「资源不在旁边」的兜底路径在资源不在旁边时一行也跑不到。
凡是「用来报告 X 出没出问题」的代码，都不能依赖 X 本身可用。

**空状态必须自证。** 一个什么都不显示的界面，让人分不清是没数据、读不到、
还是坏了。今天在菜单栏、额度页、办公室各犯了一次。

**诊断不能说谎。** 一条把 A 说成 B 的错误信息比没有错误信息更耽误事 ——
它让人以为某个方向已经排除了。凡是「写错了不会报错、只会静默失去保护」的
地方，都要配一条「必须被拒」的测试，并且**验证过那条测试真的会失败**。

**钥匙串条目跨不过版本更新。** 条目的 ACL 绑在**创建它的那个二进制**上，
而这个项目自动更新 —— 每发一版就换一次二进制。放宽 ACL 在 macOS 15+ 不管用，
数据保护钥匙串要 entitlement（未签名 CLI 拿不到）。两条都实测过。
能用的只有：更新时由旧二进制读出、新二进制写回，以及握有 CA 时自动重签。
还有一条：**SSH 会话里登录钥匙串是锁着的**，远程修不了。

**断言「没有」之前先验证检查本身。** 找不到东西时，先拿一个已知存在的目标
复验同一条命令。空结果先怀疑命令，不是先怀疑世界。

## 怎么验证你的改动

```bash
swift build && swift test
```

这也是 `llmq work review --auto` 合并前会跑的那条命令。**跑不过不会被合并。**

## 合别人的产出，一律走 `llmq work review --merge`

不要 `git merge agent/xxx`。走命令合有三件事会顺带发生，手动合一件都没有：

1. 合前跑一遍 `swift build && swift test`，跑不过直接拒
2. 落地后自动排一条【审查】任务，让另一个 agent 读完整 diff 写报告
3. 记进落地台账 —— 落地率、审查报告、储备池的真需求全从这份台账来

丢弃同理：`llmq work review --discard <分支> --reason "..."`。
**理由要写清楚**，它是下次派同类活时唯一的历史依据。手动 `git branch -D`
等于把「为什么否决」这件事永久删掉，下一个 agent 会原样再犯一遍。

实测代价：手动合并了几十次之后，落地台账显示落地率 40%，
而真实情况是大量分支既没合也没记 —— 台账失真到没法用来判断哪里在浪费。

## 你碰不得的地方

改到这些路径会被自动拦下、转人工确认（见 `SECURITY.md` 第三节）：

    *.pbxproj  Package.swift  *.sh  Tools/**  .github/**  Scripts/**  fastlane/**

不是不许改，是改了要人看一眼再放行。所以**没被要求时别顺手动它们** ——
一个「补文档注释」的任务改掉构建脚本，会让整个任务卡住等人。
