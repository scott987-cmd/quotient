# 安全设计约束

这套工具最终要**跨设备无人值守地跑 agent 改代码**。这意味着任何一个没鉴权的口子，都等价于让局域网内任何人在你的机器上执行任意代码、花你的额度、读你的全部仓库和凭据。

所以下面这些不是建议，是硬约束。改动违反其中任何一条，`swift test` 和 `llmq security` 都应该失败。

---

## 一、只允许开一个端口

**除了 `llmq cluster serve`，本工具的任何组件都不监听任何端口。**

这条规矩比它原来的样子弱。原话是"一个端口都不开，跨机一律走 SSH"，理由是**从构造上**消除"无鉴权服务"这一整类问题 —— 自造鉴权要处理常数时间比较、重放窗口、时钟漂移、密钥分发与轮换、绑定地址、半开连接，每一项都是一个可以写错的地方。

改它的原因只有一个：**要用手机派活**。iOS 上没有 SSH 这条路，而客户端证书是原生 App 连自建服务端的标准做法。

放宽的前提是这三条同时成立，缺一条这个端口就不该开：

### 1. 认证不是自己造的

用的是 TLS 双向证书，不是自己写的 HMAC。TLS 和 SSH 一样是被审了几十年的实现 —— 原来那条"不重复造轮子"的理由在这里仍然成立，没有被违反。

我们自己写的只有**校验逻辑**，而且它只有三条判定，每条都有对应的「必须被拒」测试：

- `SecTrustSetAnchorCertificatesOnly(true)` —— 不加这一行，系统根证书**也**算数，任何一张公网 CA 签的证书都能通过。这是最隐蔽也最致命的一条。
- `SecTrustEvaluateWithError` 的返回值必须真的用上，不能只调不看。
- 验过签还要查**授权名单**。私有 CA 没有吊销列表，名单是唯一的撤销手段。

服务端另外必须开 `peer_authentication_required`。漏了它，服务照常起来、请求照常处理，只是谁都能连 —— 不报错、不崩溃，静默失去全部保护。

### 2. 认证过 ≠ 想干嘛干嘛

对端能让本机做的事**穷举**在 `ClusterRequest` 那个枚举里：探活、拉看板、投任务、查任务。

没有 `exec`，没有 `shell`，没有"跑这条命令"。而且投任务时对端给的是**仓库别名**，不是路径 —— 路径由本机的 `RepoRegistry` 决定。允许对端指定绝对路径，就等于允许它在你任意目录里跑 agent。

所以一张被偷走的客户端证书，能干的最坏的事和"能往你 iCloud 收件箱丢文件的人"一样：投一个任务。那条路径的爆炸半径第五节已经写清楚了，这里**不新增**。

### 3. 只在你明确授权时才绑

`cluster serve` 默认是前台命令。`llmq cluster install-serve` 可以把它装成
launchd 常驻 —— 这看起来违反本条，其实不然：**你主动跑那条命令本身就是
那个明确授权**，和「装完默认就开着」是两回事。所以那条命令会先把它要做的事
摆出来（监听哪个端口、放行哪些节点、怎么撤销）再动手，而不是悄悄装上。

之所以需要常驻：跨机派活要求接活那台一直在听。人手工起前台进程的话，
终端一关就没了，另一台机器的任务全部堆积。绑的是内网地址不是 `0.0.0.0` —— 没有公网 IP 时两者实际暴露面一样，但显式绑定意味着多插一张网卡、开一次热点都不会把它意外带到别的网段。

### 审计会真的去敲门

`llmq security` 不是对着白名单点头 —— 白名单只能证明"这个端口是我们开的"，证明不了"它真的在拦人"。它会拿一个**不带客户端证书**的连接去请求服务，拿到回应就报严重。这是唯一能发现"双向认证被改坏了"的办法，光读配置读不出来。

对应的测试是 `ClusterNetTests`，其中 `testAnonymousProbeCanActuallySucceed` 专门证明那根探针**能够失败** —— 一条永远通过的安全测试比没有测试更糟，它给人已经测过了的错觉。

### 它保护不了什么

私钥躺在 `~/Library/Application Support/LLMQuotaBar/cluster/*.p12`，0600，口令在钥匙串里。**能读你用户账户的人就能用它**，和无口令的 SSH 私钥是同一个威胁模型 —— 无人值守的自动化本来就是这样。放钥匙串的实际收益只有一个：一份不含钥匙串的备份泄露了，用不出来。

### 仍然明确禁止

- ❌ `git daemon`（无论加不加 `--export-all` / `--enable=receive-pack`）
- ❌ 任何绑定 `0.0.0.0` / `*` 的监听
- ❌ 自造的 HTTP 控制面，哪怕加了 HMAC
- ❌ 给 `ClusterRequest` 加任何形式的"执行命令"
- ❌ 让对端指定仓库**路径**而不是别名
- ❌ 用 iCloud/共享盘传可执行内容，**除非装机命令里内嵌了 SHA-256** ——
  见下面「装机包怎么传」（快照是只读的用量数据，一直都可以）

### 装机包怎么传

`release-to-icloud.sh` 会把编译好的 `llmq` 和 `.app` 打包放进 iCloud。这直接踩了上面那条禁令，所以必须说清楚它凭什么可以：

第五节那条等式是「能往你 iCloud 写文件的人 = 能在你 Mac 上执行代码的人」。如果装机命令无条件执行 iCloud 里的东西，这条等式就从「能投一个任务，跑在隔离 worktree 里」升级成「能直接换掉你的二进制」—— 那是完全不同的量级。

所以**信任锚点被挪出了 iCloud**：发布时算出 tar 包的 SHA-256 打印在终端，装机命令里内嵌那串哈希，先校验再解包。哈希走的是你复制粘贴的那条命令，不走 iCloud。iCloud 里的内容因此退回成「不可信数据」，由另一条渠道的哈希背书；改包会导致校验失败，而不是被静默执行。

iCloud 目录里那个 `.sha256` 文件只是给你自己核对用的。**它证明不了任何事** —— 能改包的人也能改它。

能改 iCloud 又**同时**能改你看到的那条命令的人，仍然赢。但那已经是另一个量级的入侵。

更好的选择是 **AirDrop**：点对点直传，不落在任何可写的共享位置，压根不需要这套哈希把戏。发布脚本两条命令都会打印，默认推荐 AirDrop。

> 本项目设计阶段真出过一次：一个子 agent 为了验证方案，未经授权起了
> `git-daemon --export-all --enable=receive-pack` 绑在 `<内网地址>:9422`。
> 无鉴权、可写、局域网可达。这条记在这里，是为了说明这类东西有多容易被"顺手"引入。

---

## 二、仓库传输

git 远程一律用以下之一：

1. `file://`（同机）
2. `ssh://`（跨机）

不用 `git://`，不用 `http://`。

---

## 三、无人值守执行 agent 的爆炸半径

无人值守 + 跳过权限确认 = 任意代码执行。沙箱在这个场景下拦不住真正的威胁（agent 本来就以你的身份运行、能读全部凭据、网络不禁），所以靠下面这几层收敛后果，而不是假装能阻止执行：

1. **独立 worktree** —— 每个任务一个，互不干扰
2. **只允许推 `agent/*` 分支** —— hub 侧用 update 钩子强制，永远碰不到主干
3. **推送前扫密钥** —— 命中 `sk-` / `AKIA` / `ghp_` / `BEGIN PRIVATE KEY` 等完整形态就拒绝
4. **高危路径闸** —— 改到 `*.pbxproj` / `Package.swift` / `*.sh` / `Tools/**` / `.github/**` 或权限位变 755，一律转人工确认
5. **git 配置硬化** —— 执行时带
   `-c core.hooksPath=/dev/null -c core.fsmonitor=` 及
   `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null`，
   任务前后比对 `.git/config` 的 sha256

### 合成任务的输入必须是结构化事实

**禁止**把仓库里的自由文本（`TODO:` / `FIXME:` 注释、issue 正文、commit message）直接拼进 prompt。那些内容是可被写入的数据，配合跳过权限确认就是一条完整的注入链，而且不碰网络、不越权、不产生异常退出码，绕开上面所有防线。

合成任务的输入只能是结构化事实：文件路径 + 行号 + 规则 ID。

---

## 四、凭据文件

各平台的 CLI 会把 API Key 明文写进配置文件，且**默认权限不一定安全**。

实测：`~/.qwen/settings.json` 的默认权限是 `-rw-r--r--`，里面就是明文的百炼 API Key。

`llmq security` 会扫这些目录并检查权限。检测用带边界和长度约束的正则，不用子串匹配 —— 第一版用 `sk-` 做子串，`risk-` / `task-` / `disk-` 全命中，一次报出 4 条假的严重问题。会喊狼来了的安全工具等于没有。

---

## 五、iCloud 收件箱等同于远程执行入口

`iCloud Drive/LLMQuotaBar/inbox/` 里放进去的**任何文本文件都会变成一个任务**，
由常驻循环无人值守地交给 agent 执行，而 agent 是带 `--dangerously-skip-permissions` 跑的。

也就是说：**能往你 iCloud Drive 写文件的人 = 能在你 Mac 上执行代码的人。**

这不是设计缺陷，是这个功能的本质 —— 但要清楚它意味着什么：

- iCloud 账号本身就是这条链路的认证。账号被盗等同于机器被拿下，
  务必开双重认证。
- 别把这个目录通过 iCloud 共享给任何人，包括家人。
- 爆炸半径仍然受第三节那几层约束（worktree 隔离、只推 `agent/*`、
  推送前扫密钥、高危路径闸），但执行本身是无条件的。

出站方向没有这个问题：`outbox/` 只写状态，不含代码和凭据。

## 六、日常自查

```bash
llmq security
```

有严重项时退出码为 1，可以挂进 CI 或定时任务。

它检查：本工具自身是否开了端口（应为 0）、本机有哪些服务绑在局域网可达地址、macOS 应用防火墙状态、各平台凭据文件的权限。
