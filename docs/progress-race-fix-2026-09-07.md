# Flint 阶段完成误判与进度并发覆盖

## 现场与范围

2026-09-07，原 Kimi 任务 i31c8eb29e2a173f 于 10:16 正常退出，执行器把它记为 done。本轮实际只有两个文档/参考包归档提交，HEAD 为 882ba5a，并非游戏功能交付。模型真实调用 work progress --next 声明仍待复验，返回里程碑 #1902；最终 progress 却成为自动 #1903，explicitNextStepSequence 回到前一天的 #1898。终态续作判断因此正确拒绝了“旧声明”，但输入已经被覆盖。

WorkProgressStore.record 原来先读取旧值，执行多次 Git 指纹检查，再原子替换 JSON。原子替换只保证单次写不破损，并不保证两个进程读改写互斥。自动续期和 Agent 主动汇报相互交错时，新下一步、证据及检查点被旧快照覆盖，序号也会重复。

修复使用每个任务的稳定 sidecar 文件锁和同进程互斥锁；常见慢 Git 检查在锁外完成，获得锁后重新读取最新记录，合并继承字段并分配序号后原子写入。证据列表确实在期间变更时，为保证指纹与证据匹配，会在该任务锁内重新检查；其他任务不共用这把锁。主动无 next 仍清空旧声明，自动记录不制造新声明，更新时间不倒退。未改平台预留、Owner、旧协议或成果验收门槛。

现场质量线程取样还发现同一分支的 22 条历史观察逐条读取 Git。超时后的线程仍会继续，因此“出现超时日志”不能直接等同于永久停止；本批消除历史观察扫描中同一轮重复读取，失败结果也只读一次，下轮重新读取。消费架构答复并发布整改事件时仍即时读取 Git，不复用前一阶段缓存。新增反例让 Owner 在两个阶段之间真实推进孤立分支；初始缓存实现错误发布 finding，产生 4 条失败断言，最终实现必须改为过期 checkpoint。

## 验证

真实 XCTest 子进程在指纹检查后等待受控屏障，父进程发布新声明/最终清空，再释放旧自动写入；还同时释放四个进程，并以八个线程验证序号和失败写后的锁释放。最终测试在独立旧实现副本上出现 19 条失败断言，原仓库实现没有为变异临时改回旧代码。日期比较使用双方实际持久化后的值，保持协议的 ISO 秒精度，避免把内存毫秒当字段丢失。

质量扫描回归在旧实现中实际读取 22 次而失败；修复后同轮一次，下一轮真实改变 Git 后重新读取并发布相应问题。原阶段范围、旧报告拒绝、跨 Owner 保守等用例随针对性 41 项通过。随后增加的线程失败写回归纳入全量运行。

服务端完整清单 1556 项：1554 通过、0 失败、2 项已有钥匙串交互测试跳过，未把跳过算通过。这一完整运行绑定 source-fingerprint.json。运行期间发现并修正上述跨阶段缓存风险；最终候选绑定 source-fingerprint-final.json，受影响的完整 StageFindingLoopTests 21 项重跑全部通过（含新增真实 Git 推进反例）。这是全量验证加最终差量复验，未声称完整清单在最后一行修改后又重跑一次。

本批详细日志及源码指纹位于 qa-reports/2026-09-07-progress-race。独立 iPhone/iPad、通用编译、签名发布和生产恢复分别记录，不能以其中一项替代其余项目。

## 实际交接

Codex 读取了本轮两提交的 diff，并检查当前资产对应的 fire-raise-48s.jpg 与 walk-with-rifle-38s.jpg：干员仍为 T恤短裤赤脚，举枪帧左手明显不接触护木。BREAKWATER 样板的认可不等于这些不同缺陷也获认可。恢复前准备了带任务版本、Owner、原执行轮次和 Git HEAD 校验的维护操作，保留原分支、工作区、会话、质量历史与平台配置，仅纠正本次错误终态，并把真实画面复核与可在模拟器推进的具体接入工作交回原任务。没有伪造用户回答或生产进度，也没有把旧 Kimi 执行记录改成仍在运行。
