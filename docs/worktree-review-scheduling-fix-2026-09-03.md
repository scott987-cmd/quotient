# Flint M2 阶段评审漏调度修复（2026-09-03）

## 现场事实

- M2 的主任务 `i31c8eb29e2a173f` 在 02:39:55 结束执行；最后提交 `2840674`，并非已通过最终验收。
- 三条 `milestone-eyes` 在原始账本中为 queued，但 scheduler-current 为 idle / pendingTaskCount=0。
- 实现任务 repo 为 `/Users/dushibing/Developer/Flint`，阶段评审 repo 为工作副本 `worktrees/flint-kimi`。两者的 Git common-dir 相同。
- 旧专注过滤只比较路径字符串，既漏掉评审，也让权威快照错误显示空队列。
- 同一差异存在于仓库租约：直接放宽专注过滤而不统一租约，会允许主仓与工作副本并发改同一仓库。

## 修复边界

1. RepoLease 的项目归一同时供专注范围、互斥锁和任务摄入使用；linked worktree 按本机 Git common-dir 归主仓。
2. 不按目录名、父目录前缀、远程地址或相同提交历史认定同一项目；Git 探测有 2 秒超时，失败保持原路径。
3. 新建和预构造任务在统一摄入入口归一；checkpoint 从工作副本取证，但归属原任务主项目。
4. 协调器冻结快照前，按 revision/CAS 修复已存在的未领取 queued 记录。保留任务 ID、Owner、分支、提示词及幂等键；不动运行中、已领取、暂停或冻结任务，不复活历史。
5. 未改 Flint 代码、未改实现 Owner、未批准样板合入。两条被最新证据取代的旧评审仅标记作废，历史证据保留。
6. 同分支证据换版与定时清理共用引用集合：排队/运行/阻塞评审、已发布摘要和未确认 checkpoint 引用的媒体都保留，不能因为主任务结束或新提交只有文档就删图。
7. 全量回归另外检出冷却事件顺序缺陷：ISO8601 秒精度把同秒 `record → clear` 压成同一时间，随机 UUID 排序可让已解除冷却复活。事件新增兼容的亚秒排序字段；旧字段仍保留、旧数据可读。旧事件时间完全相同时采用 clear 最后生效，新的后续失败仍按真实亚秒顺序重新阻断，不抹掉真实新故障。

## 已执行的现场恢复

- 先验证 `b57dbee`、`95668c9` 都是最新证据提交 `7d83434` 的祖先，再通过正常任务接口作废两条旧阶段评审。它们没有运行进程或工作分支；没有删除 Flint 工作副本或 Git 产出。
- 最新评审引用的四张共享 JPG 当时全部缺失，原始 PNG 仍在 `7d83434e783d545442b94c2833f84c8c9a3abfb9`。已从该提交取回 `fullbody-infected`、`a-front`、`b-45`、`e-face`，按现有管线恢复原共享文件名；未修改画面内容。
- 已复现两处不检查待消费引用的清理路径；没有删除审计日志能断言现场四张图具体由哪一次清理删除，因此只确认漏洞和当前缺图事实，不虚构删除时间/调用方。
- M2 自动落地原为关闭，修复保持关闭；恢复阶段评审不代表批准最终合入。
- 部署前发现 M2 缺少 `mmx` 可执行文件及认证配置。安装与本机一致的 `mmx-cli@1.0.19`（用户级路径，关闭安装脚本），经 SSH 复制现有 MiniMax 专用配置到原先不存在的位置，权限 `0600`，未覆盖任何其他订阅配置。M2 ARM Node 实测运行成功，`auth status` 确认 API-key 方式，额度查询返回成功、通用窗口剩余 99% / 周剩余 97%；没有另外申请或购买额度。

## 验证记录

- 原实现运行 `WorktreeSchedulingTests`：6 项中出现 16 个断言失败，真实复现 pending=0、作用域漏匹配及双重仓库锁。
- 首轮修复后相关 57 项测试通过（包括 6 项真实 Git/worktree 回放）。
- 最终新增 8 项 `WorktreeSchedulingTests` 全通过；Housekeeping 3 项与曾失败的 TLS 慢处理回归单独运行共 4 项通过。
- 反向撤销专注归一、未确认 checkpoint 引用保护、缓存引用保护后：11 项测试出现 12 个断言失败；随后恢复全部修复。测试确实能检出对应故障。
- 第一轮全量发生一项 TLS 身份导入/等待超时，后续为重新验证最终补丁而主动结束该轮，不能记为通过；该 TLS 组在后续单独及全量验证中均通过。
- 第二轮串行全量跑完 1,365 项，2 项跳过、1 项失败（冷却恢复墓碑复活），TLS 组及其余回归通过。新增两个固定时间/逆序 ID 的磁盘往返用例在旧实现上稳定产生 3 个断言失败，确认不是用随机重跑掩盖问题；修复后的验证见下一项。
- 最终 `swift build` 成功，32 项定向回归通过；`swift test --parallel --num-workers 4` 执行至 1,367 / 1,367，退出码 0，无失败报告。日志：`/tmp/llmq-recovery-final-build.log`、`/tmp/llmq-recovery-focused-green.log`、`/tmp/llmq-recovery-final-parallel.log`。这轮已包含冷却顺序修复，而不是沿用前一轮失败结果。

## 现场恢复目标

保留最新评审 `i768bf84bccccf0c`（证据 `7d83434`），作废其已被替代的 `i4a48f32ae944394` / `id0f0221540a4125`。验证任务归回主仓、调度快照不再漏计、真实评审执行器启动并产生有效结果。独立评审的结论不能被调度修复冒充。

## 发布与现场结果

- 已发布/安装通用修复包 `0422114194a8f0471c05f4753e3009dc3f02ecbdc8dc5f7a4245fe546b944a3c`。三机 SSH 实查 CLI SHA-256 都是 `2d054a45bbec6ee184dfe7c791d50a61e66d4a40fc58f342d4bb066b1c0ffeab`，桌面 App 都是 `9a8fa4e0ae4dbaa631e50c1a7e1d281c6ee38c6ac58968e83ad38d7838205b91`。首次发布因回执尚未收齐退出 3；M2 云盘延迟时传送同一签名包并正常验签安装，最终 `llmq release verify --wait-seconds 30` 退出 0，三机确认收齐。
- M2 协调器日志实录“已归一 1 条排队任务的项目身份”。权威快照变为 `dispatching / pendingTaskCount=1`，不再错误显示 idle/0；已有任务原 ID 不变。
- 最新评审由 `minimax.media` 于 08:54:33 至 08:55:34 执行，61 秒，只有一次真实尝试。实际观察日志包含四张图的逐图描述；输出 `reviews/EVAL-视觉-i768bf84bccccf0c.md`，提交 `a98e11f901571887f4a8c73f4dbacbbc7204e147`，1 个文件/1 个新提交，非零产出。
- 制作任务仍为 Kimi、分支 `agent/kimi/i31c8eb29e2a173f@2840674`，工作区干净；主线仍是 `35d6fbf`，自动合入关闭。没有重启制作、没有合入资产。评审执行器退出后协调器已回到 `idle / active=[] / pendingTaskCount=0`，这次空闲是评审确实结束，不是漏调度。
- M2 任务板在 08:57:08 重新发布，采集器报告镜像刚成功。未直接查看用户实体手机界面，因此不声称已肉眼确认手机渲染。

## 仍须区分的边界

- 评审报告结论为“未达标”，但它把本次阶段模型截图与整局游戏门槛混用：以没有 HUD/握枪/连续录屏否决母版截图，甚至把 A-pose 参考图直接等同于游戏里 T-pose。这些不是本轮截图能成立的最终结论，不能据此自动返工。
- 四图可提示服装缺失、皮肤/头发观感等问题；是否达到完整游戏交付要求必须把制作分支已有录屏、当前阶段目标和正式验收范围对齐后判断。此次只恢复调度与读图，不把“评审跑通”写成“游戏质量已通过”。阶段观察和正式验收标准的隔离仍是后续需校正的问题。
- 本次 WorkAttempt 有真实耗时与提交，但没有输入/输出 token 字段；采集器展示 MiniMax 的 0 次/0 token 不代表本次五次模型调用没有消耗，不能据此报零成本。
