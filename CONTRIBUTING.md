# 参与进来

## 先说清楚这个项目的脾气

这套工具**无人值守地跑 agent 改代码、花真钱**。所以有些在别处算讲究、
在这里是硬要求的东西：

1. **失败必须留痕。** 本项目最贵的几个 bug 全是一个形状：`try?` 把错误吞了，
   症状是「跑通了但没生效」。查这种问题比查崩溃贵十倍 —— 崩溃有堆栈，
   静默跳过只有一个说不清的现象。状态文件的解码走 `SafeDecode`。
2. **跨机同步的结构体要对缺字段免疫。** Swift 合成的 `Decodable`
   **不用属性默认值** —— 加一个字段就能让还没升级的机器整份快照解不出来。
   加字段时手写 `init(from:)`，新字段用 `decodeIfPresent`。
3. **注释写「为什么」，不写「是什么」。** 代码说得清做了什么，
   说不清当初为什么这么选、踩过什么坑。后者才是别人（和三个月后的你）
   真正需要的。
4. **安全约束见 `SECURITY.md`。** 那里面的不是建议，是硬约束 ——
   违反了 `swift test` 和 `llmq security` 都该失败。

## 怎么验证你的改动

```bash
swift build && swift test
```

这也是自动合并前会跑的那条命令，跑不过不会被合。

想在干净环境里试，不弄脏自己的配置：

```bash
LLMQ_HOME=/tmp/试一下 swift run llmq collect
```

## 提 PR 之前

- [ ] `swift test` 全绿
- [ ] 新加的行为有测试。**尤其是「会自动花钱」的逻辑** ——
      派活、填空窗、冷却判定，这些错了是真金白银
- [ ] 没有硬编码你自己的路径、机器名、账号
- [ ] 改了跨机同步的结构体？确认老版本写的文件还能解开

## 报 bug

最有用的一份 bug 报告包含：

```bash
llmq doctor      # 本机探测到什么、哪些配置漂移了
llmq mirror      # 多机同步的新鲜度，以及解不出来的文件
```

这两条的输出通常直接指向问题。**注意它们会打印你的机器名和仓库路径**，
贴出来之前自己看一眼。
